export async function traer_api(){
    try{
        const atrr= [];
        for(let i=1; i<16; i++){
            const urlcategorie = await fetch("https://api.chucknorris.io/jokes/random")
            return JSON();
			};
			return;
	} catch (error) {
		console.log(error);
	}
}